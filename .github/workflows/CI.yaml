---

name: CI
on:
  release:
    type:
      - created
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
  schedule:
    # Weekly on Sundays:
    - cron: '00 04 * * 0'

  workflow_dispatch:

jobs:
  test:
    name: Test
    runs-on: ubuntu-18.04
    steps:
      - run: echo 'test'
